## What this changes

<!-- One paragraph: the behaviour before and after. -->

## Issue

<!--
Fixing a bug? It must already have an issue — standing rule: the issue is opened
BEFORE the fix, so "what broke?" and "is it fixed?" stay answerable after the branch
is gone. Search `gh issue list -R <owner>/<repo> --state all --search "..."` first;
a closed issue means it was resolved once already.

One `Fixes #N` line per issue this PR closes — a batched fix closes several.
Not a bug fix? Delete this section.
-->

Fixes #

## How this was verified

<!-- The test that fails without this change, the command you ran, or the layer you
     read to prove it. "CI is green" is not verification of a behaviour. -->
